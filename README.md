# Multi-Container-Application

## Setup
1. Clone the repository
   ```bash
      git clone "https://github.com/Polyston/Multi-Container-Application.git"
      cd Multi-Container-Application
   ```

2. To run the application
   ```bash
      docker compose up --build
   ```
